map $http_upgrade $connection_upgrade {
  default upgrade;
  ''      close;
}

map $http_cf_connecting_ip $pi_client_ip {
  default $http_cf_connecting_ip;
  ''      $remote_addr;
}

upstream mastodon_web {
  server web:3000;
  keepalive 16;
}

upstream mastodon_streaming {
  server streaming:4000;
  keepalive 16;
}

# Windows keeps the resident tokens under DPAPI. Docker Desktop's verified
# host.docker.internal bridge reaches this loopback-only host process without
# exposing the MCP port on the LAN.
upstream cmx_mcp {
  server host.docker.internal:8766;
  keepalive 8;
}

server {
  listen 80;
  server_name _;
  server_tokens off;

  client_max_body_size 100m;
  proxy_connect_timeout 30s;
  proxy_send_timeout 300s;
  proxy_read_timeout 300s;

  location = /_pi/health {
    access_log off;
    add_header Cache-Control "no-store" always;
    default_type text/plain;
    return 200 "PI OS OK\n";
  }

  location = /_pi/mcp-health {
    proxy_pass http://cmx_mcp/_cmx/mcp-health;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_buffering off;
  }

  location = /.well-known/oauth-authorization-server {
    proxy_pass http://cmx_mcp;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_buffering off;
  }

  location ~ ^/\.well-known/oauth-protected-resource/mcp/[a-z0-9_-]+$ {
    proxy_pass http://cmx_mcp;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_buffering off;
  }

  location = /clipboard {
    return 302 /clipboard/;
  }

  location = /clipboard-site-switch.js {
    alias /srv/clip-brain/site-switch.js;
    default_type application/javascript;
    add_header Cache-Control "no-store" always;
    add_header X-Content-Type-Options "nosniff" always;
  }

  location ^~ /clipboard/ {
    alias /srv/clip-brain/;
    index index.html;
    add_header Cache-Control "no-store" always;
    add_header Referrer-Policy "no-referrer" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Content-Security-Policy "base-uri 'none'; default-src 'self'; frame-ancestors 'none'; form-action 'self'; object-src 'none'; img-src 'self' data:; media-src 'self' blob:; connect-src 'self'; style-src 'self'; script-src 'self'; worker-src 'self' blob:" always;
  }

  # Clipboard's own API. The body cap sits deliberately ABOVE the 1 GiB
  # per-entry limit so an oversized upload is rejected by the application with
  # a JSON error the page can show, instead of by Nginx with a bare 413 page.
  location ^~ /clipboard-api/ {
    client_max_body_size 1200m;
    proxy_pass http://cmx_mcp;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_request_buffering off;
    proxy_buffering off;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
  }

  location ^~ /files/ {
    client_max_body_size 2g;
    proxy_pass http://cmx_mcp;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_request_buffering off;
    proxy_buffering off;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
  }

  location ~ ^/(authorize|token|register|revoke|oauth/invite|mcp/[a-z0-9_-]+)$ {
    client_max_body_size 1m;
    proxy_pass http://cmx_mcp;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_read_timeout 300s;
  }

  location ^~ /api/v1/streaming {
    proxy_pass http://mastodon_streaming;
    proxy_http_version 1.1;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port 443;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Proxy "";

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering off;
    proxy_redirect off;
    proxy_read_timeout 86400s;
    tcp_nodelay on;
  }

  # A service worker is an asset manifest in executable form: after a Mastodon
  # image upgrade, an old cached /sw.js imports hashed chunks that no longer
  # exist in the new container. Never let Cloudflare hold it for Mastodon's
  # upstream seven-day max-age; every load may cheaply revalidate this one file.
  location = /sw.js {
    proxy_pass http://mastodon_web;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port 443;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Connection "";
    proxy_hide_header Cache-Control;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
  }

  location / {
    proxy_pass http://mastodon_web;
    proxy_http_version 1.1;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $pi_client_ip;
    proxy_set_header X-Forwarded-For $pi_client_ip;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port 443;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Proxy "";

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering on;
    proxy_redirect off;
    tcp_nodelay on;

    # CMX widgets: sub_filter needs an uncompressed upstream response;
    # Cloudflare re-compresses toward visitors.
    proxy_set_header Accept-Encoding "";
    # Cloudflare may still hold the previous Mastodon image's /sw.js under the
    # bare URL. Patch register() before the application bundle runs so this
    # version gets a fresh cache key immediately. Bump cmx-sw whenever the
    # pinned Mastodon image changes; the exact /sw.js location above prevents
    # the new response from becoming another week-long edge object.
    sub_filter '<head>' '<head><script>(function(){if(!navigator.serviceWorker){return;}var original=navigator.serviceWorker.register.bind(navigator.serviceWorker);navigator.serviceWorker.register=function(url,options){if(String(url)==="/sw.js"){url="/sw.js?cmx-sw=4.6.4-1";}return original(url,options);};})();</script>';
    sub_filter '</body>' '<script src="/files/voice.js?cmx-v=20" defer></script><script src="/clipboard-site-switch.js" defer></script></body>';
    # Mastodon draws "search unavailable on this instance" straight from this
    # flag and never issues /api/v2/search while it is false. Elasticsearch stays
    # off; the version-locked Rails initializer mounted into web lets only the
    # explicitly configured local Owner answer status searches with PostgreSQL.
    sub_filter '"search_enabled":false' '"search_enabled":true';
    sub_filter_once on;

    # CMX widget CSP relaxation. Mastodon sends a strict CSP whose script-src /
    # style-src are pinned to per-response nonce / hash allow-lists. The injected
    # same-origin scripts and inline widget styles carry no nonce, so the browser
    # would reject them. This private single-user instance re-issues an otherwise
    # strict policy with only same-origin + inline script/style relaxed.
    proxy_hide_header Content-Security-Policy;
    add_header Content-Security-Policy "base-uri 'none'; default-src 'none'; frame-ancestors 'none'; form-action 'self'; font-src 'self' data: https://pi.ler428.xyz; img-src 'self' data: blob: https://pi.ler428.xyz; media-src 'self' data: blob: https://pi.ler428.xyz; manifest-src 'self' https://pi.ler428.xyz; child-src 'self' blob: https://pi.ler428.xyz; worker-src 'self' blob: https://pi.ler428.xyz; connect-src 'self' data: blob: https://pi.ler428.xyz wss://pi.ler428.xyz; frame-src 'self' https:; style-src 'self' 'unsafe-inline' https://pi.ler428.xyz; script-src 'self' 'wasm-unsafe-eval' 'unsafe-inline' https://pi.ler428.xyz" always;
  }
}
